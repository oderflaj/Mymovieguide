import { StyleSheet } from "react-native";

export default new StyleSheet.create({
  container: {
    borderWidth: 1,
    alignItems: "center",
    height: "100%",
    justifyContent: "center",
  },
});
